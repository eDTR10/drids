# Committed on purpose: pins production builds (npm run build / npm run
# deploy) to the public API, so a developer's local .env pointing at a LAN IP
# can never accidentally ship to the live GitHub Pages site.
VITE_BACKEND_URL=https://api.yt-jam.com
